# การส่งโค้ดขึ้น repo นี้

ระบบนี้เก็บข้อมูลจริงของห้องวิจัยไว้ใน Google Sheets และถือกุญแจเข้าปฏิทินของ
สมาชิกทุกคน กติกาข้างล่างจึงเน้นสองเรื่อง: **อย่าให้ความลับหลุด** และ
**อย่าทำข้อมูลที่มีอยู่เสียหาย**

## ก่อน push ทุกครั้ง

CI รันสี่อย่างนี้ให้อัตโนมัติ แต่รันเองก่อนจะเร็วกว่ารอ:

```bash
npm run lint
npx tsc --noEmit
npm test
npm run build
```

**ทั้งสี่อย่างผ่านยังไม่พอ** — บั๊กที่เจ็บที่สุดในโปรเจกต์นี้ผ่านทั้งสี่ด่าน
มาแล้วสองครั้ง (ดู `CLAUDE.md` หัวข้อ `'use server'`) ถ้าแก้อะไรที่เห็นผลบนหน้าเว็บ
**ต้องเปิดหน้านั้นจริง** ก่อน push

## ห้ามขึ้น git เด็ดขาด

- `.env.local` และไฟล์ `.env*` อื่น ยกเว้น `.env.example`
- ไฟล์ service account key (`*.json` ที่ดาวน์โหลดจาก Google Cloud)
- Client secret, refresh token, หรือค่าใดที่ขึ้นต้นด้วย `GOCSPX-`
- ข้อมูลจริงของสมาชิก เช่น อีเมล ชื่อ หรือเนื้อหาการประชุม ในไฟล์ทดสอบ

`.gitignore` ครอบให้แล้ว แต่ `git add -f` ทะลุได้ และคีย์ที่พิมพ์ลงในโค้ดโดยตรง
ไม่มีอะไรกัน ถ้าเผลอ commit ความลับไปแล้ว **ให้ถือว่าค่านั้นตายทันที** ต้องไป
สร้างใหม่ที่ Google Cloud ก่อน แล้วค่อยจัดการประวัติ git — ลบ commit อย่างเดียว
ไม่พอ เพราะค่าเดิมอาจถูกดึงไปแล้ว

## แก้โครงสร้างฐานข้อมูล

อย่าใช้ `db:init` กับชีตที่มีข้อมูล มันเขียนทับแถวหัวตารางโดยแถวข้อมูลไม่ขยับ

```bash
npm run db:migrate-schema -- --dry-run   # ดูแผนก่อน
npm run db:migrate-schema                # ลงมือ
```

เพิ่มคอลัมน์ใหม่ **ต่อท้าย** อาร์เรย์ใน `SCHEMAS` เสมอ แล้วบั๊ม `SCHEMA_VERSION`

## ทดสอบกับชีตจริง

ชีตที่ต่ออยู่คือข้อมูลจริง ไม่ใช่ sandbox

- สร้างแถวทิ้ง ทดสอบ แล้วลบให้หมด **และตรวจซ้ำว่าลบจริง**
- สร้างบัญชีชั่วคราวด้วยรหัสสังเคราะห์ อย่าใช้บัญชีของคนจริง
- ใช้ UI ของแอปเอง เพื่อให้ `row_version` และ `audit_log` ทำงานตามปกติ

## Branch และการ merge

`master` คือสิ่งที่ใช้งานจริง งานที่ไม่ใช่การแก้เล็กน้อยควรแยก branch:

```bash
git switch -c fix/ชื่องาน
```

รอ CI เขียว แล้วค่อย merge

## ข้อความ commit

อธิบาย **ว่าทำไม** ไม่ใช่ว่าแก้อะไร — diff บอกอยู่แล้วว่าแก้อะไร สิ่งที่ diff
บอกไม่ได้คือเหตุผลที่เลือกทางนี้ และอะไรที่ลองแล้วไม่เวิร์ก

ถ้าแก้บั๊ก ให้เขียนไว้ว่าอาการเป็นอย่างไรและอะไรจับมันได้ — เพื่อให้คนถัดไป
รู้ว่าด่านไหนเชื่อถือได้แค่ไหน

## อ่านก่อนเริ่ม

`CLAUDE.md` รวมกับดักที่เคยทำพลาดจริงในโปรเจกต์นี้ไว้ พร้อมเหตุผลว่าทำไมมันไม่ถูก
จับโดยเครื่องมือปกติ ส่วน `AGENTS.md` เป็นไฟล์ที่ `next dev` เขียนทับเอง
อย่าแก้
